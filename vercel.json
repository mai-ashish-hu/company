{
  "framework": "remix",
  "buildCommand": "cd ../.. && npm ci && npm run build --workspace=@careernest/web-company",
  "outputDirectory": "build",
  "installCommand": "echo 'skip — workspace install handled in buildCommand'",
  "build": {
    "env": {
      "APP_BASE_PATH": "/"
    }
  }
}
